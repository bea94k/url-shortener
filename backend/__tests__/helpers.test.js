//const helpers = require('../helpers');
// use: helpers.stripUrl

//const stripUrl = require('../helpers').stripUrl;
// use: stripUrl

const {
  randomChars,
  stripUrl,
  domainOfUrl,
  isDomainValid,
} = require("../helpers");
// use: stripUrl
